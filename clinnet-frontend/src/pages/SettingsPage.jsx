import React from 'react';

const SettingsPage = () => {
    return (
        <div>
            <h2>Settings (Admin)</h2>
            <p>This page allows administrators to configure application settings.</p>
            {/* TODO: Implement settings form (e.g., clinic info, integrations) */}
        </div>
    );
};

export default SettingsPage;