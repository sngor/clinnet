{
  "name": "clinnet-emr",
  "version": "1.0.0",
  "description": "Clinnet EMR Backend Services",
  "scripts": {
    "test": "pytest",
    "deploy": "sam build && sam deploy",
    "start-local": "sam local start-api",
    "seed-data": "python scripts/seed_data.py"
  }
}